# Certificate Tool

Утилита для работы с сертификатами и контейнерами ЭЦП в Windows.

Программа позволяет:
- просматривать сертификаты в файловой системе;
- переименовывать файлы сертификатов по шаблону;
- удалять просроченные сертификаты;
- просматривать сертификаты в хранилищах Windows (реестр);
- работать с контейнерами ЭЦП (CryptoPro / ViPNet / Rutoken);
- находить просроченные контейнеры и удалять их;
- копировать служебные данные контейнеров (серийный номер, отпечаток и др.).

---

## ⚠ ВАЖНО

Утилита выполняет операции с криптографическими ключами и сертификатами.

**Ответственность за использование программы полностью лежит на пользователе.**

Рекомендуется:
- предварительно создавать резервные копии;
- внимательно проверять список операций перед удалением;
- запускать программу от имени администратора при работе с LocalMachine.

---

## 📂 Вкладки программы

### 1. Файлы сертификатов

Позволяет работать с файлами сертификатов в каталогах (`.cer`).

Возможности:
- выбор папки и поиск файлов сертификатов;
- фильтрация по CN (владелец сертификата);
- подсветка просроченных сертификатов красным цветом;
- переименование файлов по шаблону:
  - Фамилия Имя Отчество
  - Фамилия И.О.
  - без даты
  - с датой окончания срока действия
  - с датой начала и окончания;
- удаление всех просроченных файлов сертификатов.

---

### 2. Реестр (хранилища Windows)

Работа с системными хранилищами сертификатов Windows:

Поддерживаемые хранилища:
- CurrentUser
- LocalMachine (требует запуск от администратора)

Типы хранилищ:
- MY
- CA
- ROOT

Функции:
- просмотр сертификатов;
- поиск по CN;
- сортировка по столбцам;
- подсветка просроченных сертификатов;
- удаление всех просроченных сертификатов.

Если программа запущена не от имени администратора, чекбокс **LocalMachine** будет недоступен.

---

### 3. Контейнеры ЭЦП (CSP)

Работа с контейнерами закрытых ключей (CryptoPro, ViPNet, Rutoken).

Отображаемые данные:
- Scope (CurrentUser / LocalMachine);
- Провайдер CSP;
- Имя контейнера;
- Уникальное имя контейнера (PP_UNIQUE_CONTAINER);
- Владелец (CN);
- Дата начала действия сертификата;
- Дата окончания действия сертификата;
- Статус (Действующий / Срок действия истек / Без сертификата);
- Серийный номер сертификата;
- Отпечаток сертификата;
- Экспорт закрытого ключа (+ / -);
- Сертификат в контейнере (+ / -).

Дополнительно:
- поиск по CN, имени контейнера и уникальному имени;
- подсветка просроченных контейнеров красным;
- подсветка контейнеров без сертификата серым;
- чекбокс «Показывать контейнеры без сертификата».

---

## 🖱 Контекстное меню (правая кнопка мыши) для контейнеров

Доступно:
- Копировать серийный номер;
- Копировать отпечаток;
- Сделать контейнер экспортируемым;
- Удалить выбранные контейнеры;
- Удалить просроченные контейнеры.

---

## 🔍 Поиск и сортировка

Во всех вкладках доступно:
- поле поиска по CN;
- сортировка по любому столбцу кликом по заголовку;

---

## 🧩 Требования

- Windows 10/11
- Python 3.10+ (64-bit рекомендуется)
- Установленные CSP:
  - CryptoPro CSP
  - ViPNet CSP
  - драйверы Rutoken

Зависимости Python:

```bash
pip install cryptography
```

---

## ▶ Запуск

```bash
python cer_tool_gui.py
```

Для работы с LocalMachine:

```bash
Запустить от имени администратора
```

---

## 📦 Сборка в exe (опционально)

Можно упаковать в exe с помощью PyInstaller:

```bash
pip install pyinstaller
pyinstaller --onefile --noconsole cer_tool_gui.py
```

Готовый файл появится в папке `dist`.

---

## 📝 Лицензия и ответственность

Программа предоставляется «как есть».

Автор не несёт ответственности за:
- потерю ключей;
- удаление сертификатов;
- невозможность восстановления контейнеров;
- последствия использования утилиты.

Используйте на свой страх и риск.

---

## 📌 Примечания

- Контейнеры без сертификатов могут не иметь срока действия.
- Не все CSP поддерживают экспорт закрытого ключа.
- Некоторые операции могут быть запрещены политиками безопасности токена.
